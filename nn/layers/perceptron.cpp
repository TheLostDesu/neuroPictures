#include <vector>
#include "utility.cpp"

class perceptronLayer 
{
    public:
        std::vector <float> predict(std::vector<float> layer_in) {
            
        }
    private:
        matrix weights;
};